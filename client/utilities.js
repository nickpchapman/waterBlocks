document.getElementById('submit')
  .addEventListener('click', () => {
    handleClick();
  });

const handleClick = () => {
    let walls = document.getElementById('walls').value
    getNewMap(walls)
};

const getNewMap = () => {

};

const renderMap = () => {

};

const renderRow = () => {

};
